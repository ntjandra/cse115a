const firebaseConfig = {
    apiKey: "replaceThat AND NEVER push that",
    authDomain: "replaceThat AND NEVER push that",
    databaseURL: "replaceThat AND NEVER push that",
    projectId: "replaceThat AND NEVER push that",
    storageBucket: "replaceThat AND NEVER push that",
    messagingSenderId: "replaceThat AND NEVER push that",
    appId: "replaceThat AND NEVER push that",
    measurementId: "replaceThat AND NEVER push that"
};

export default firebaseConfig;